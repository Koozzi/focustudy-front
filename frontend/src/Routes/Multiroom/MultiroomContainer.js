import React, { useLayoutEffect } from 'react'
import MultiroomPresenter from './MultiroomPresenter';

export default () => {
   
    useLayoutEffect(() => {
        
    }, []);
    
    
    
    return(
        <MultiroomPresenter />
    )
}
